<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Test</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <style>
      :root {
        --bg-color: #111827;
        --card-bg: #1f2937;
        --card-inner: #283548;
        --primary-color: #3b82f6;
        --primary-hover: #60a5fa;
        --accent-color: #8b5cf6;
        --accent-hover: #a78bfa;
        --text-color: #e5e7eb;
        --text-muted: #9ca3af;
        --input-bg: #374151;
        --input-border: #4b5563;
        --input-focus: #60a5fa;
        --border-radius: 8px;
        --shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.3),
          0 4px 6px -2px rgba(0, 0, 0, 0.15);
        --transition: all 0.3s ease;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background-color: var(--bg-color);
        color: var(--text-color);
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        padding: 20px;
        background-image: linear-gradient(
            to bottom right,
            rgba(30, 41, 59, 0.8),
            rgba(17, 24, 39, 0.9)
          ),
          radial-gradient(
            circle at top right,
            rgba(79, 70, 229, 0.1),
            transparent 400px
          ),
          radial-gradient(
            circle at bottom left,
            rgba(59, 130, 246, 0.1),
            transparent 400px
          );
        background-size: cover;
        background-repeat: no-repeat;
        background-attachment: fixed;
      }

      .edit-container {
        width: 90%;
        max-width: 900px;
        background: var(--card-bg);
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
        overflow: hidden;
        position: relative;
        transition: var(--transition);
        margin: 20px auto;
      }

      .edit-container:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 25px -5px rgba(0, 0, 0, 0.4),
          0 10px 10px -5px rgba(0, 0, 0, 0.2);
      }

      .edit-header {
        background: linear-gradient(135deg, #3b82f6 0%, #4f46e5 100%);
        padding: 30px 20px;
        text-align: center;
        color: white;
        position: relative;
        border-radius: var(--border-radius) var(--border-radius) 0 0;
      }

      .edit-header:before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-image: url("data:image/svg+xml,%3Csvg width='30' height='30' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M11 18c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm48 25c3.866 0 7-3.134 7-7s-3.134-7-7-7-7 3.134-7 7 3.134 7 7 7zm-43-7c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zm63 31c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zM34 90c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zm56-76c1.657 0 3-1.343 3-3s-1.343-3-3-3-3 1.343-3 3 1.343 3 3 3zM12 86c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm28-65c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm23-11c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm-6 60c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm29 22c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zM32 63c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm57-13c2.76 0 5-2.24 5-5s-2.24-5-5-5-5 2.24-5 5 2.24 5 5 5zm-9-21c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM60 91c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM35 41c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2zM12 60c1.105 0 2-.895 2-2s-.895-2-2-2-2 .895-2 2 .895 2 2 2z' fill='%23ffffff' fill-opacity='0.1' fill-rule='evenodd'/%3E%3C/svg%3E");
        opacity: 0.7;
        border-radius: var(--border-radius) var(--border-radius) 0 0;
      }

      .edit-header h2 {
        font-weight: 600;
        font-size: 28px;
        margin-bottom: 5px;
        position: relative;
        text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
      }

      .edit-header p {
        opacity: 0.9;
        font-size: 16px;
        position: relative;
      }

      .back-button {
        position: absolute;
        top: 15px;
        right: 15px;
        background-color: rgba(255, 255, 255, 0.2);
        border: none;
        border-radius: 50%;
        width: 36px;
        height: 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 16px;
        cursor: pointer;
        color: white;
        transition: var(--transition);
        z-index: 10;
      }

      .back-button:hover {
        background-color: rgba(255, 255, 255, 0.3);
        transform: scale(1.1);
      }

      .test-info {
        background-color: var(--card-inner);
        padding: 15px 30px;
        display: flex;
        justify-content: space-between;
        flex-wrap: wrap;
        border-bottom: 1px solid var(--input-border);
      }

      .test-info p {
        margin: 5px 0;
        font-size: 14px;
        color: var(--text-color);
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .test-info p i {
        color: var(--primary-color);
      }

      .text-green-500 {
        color: #10b981 !important;
      }

      .text-red-500 {
        color: #ef4444 !important;
      }

      .edit-form {
        padding: 30px;
      }

      .form-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 20px;
        margin-bottom: 20px;
      }

      .card {
        background-color: var(--card-inner);
        border-radius: var(--border-radius);
        padding: 20px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        transition: var(--transition);
      }

      .card:hover {
        box-shadow: 0 6px 8px rgba(0, 0, 0, 0.15);
        transform: translateY(-2px);
      }

      .card-header {
        display: flex;
        align-items: center;
        margin-bottom: 15px;
        gap: 10px;
      }

      .card-header i {
        color: var(--primary-color);
        font-size: 18px;
      }

      .card-header h3 {
        font-size: 16px;
        font-weight: 500;
        color: var(--text-color);
      }

      .input-group {
        margin-bottom: 20px;
        position: relative;
      }

      .input-group:last-child {
        margin-bottom: 0;
      }

      .input-group label {
        display: block;
        margin-bottom: 8px;
        font-weight: 500;
        color: var(--text-color);
        font-size: 14px;
      }

      .input-group input,
      .input-group select,
      .input-group textarea {
        width: 100%;
        padding: 12px 15px;
        border: 1px solid var(--input-border);
        border-radius: var(--border-radius);
        background-color: var(--input-bg);
        font-size: 14px;
        color: var(--text-color);
        transition: var(--transition);
      }

      .input-group input:focus,
      .input-group select:focus,
      .input-group textarea:focus {
        outline: none;
        border-color: var(--input-focus);
        box-shadow: 0 0 0 3px rgba(96, 165, 250, 0.2);
      }

      .input-group input::placeholder,
      .input-group select::placeholder,
      .input-group textarea::placeholder {
        color: var(--text-muted);
      }

      .input-group i {
        position: absolute;
        right: 15px;
        top: 38px;
        color: var(--text-muted);
      }

      .divider {
        text-align: center;
        margin: 25px 0;
        position: relative;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .divider:before,
      .divider:after {
        content: "";
        flex: 1;
        height: 1px;
        background-color: var(--input-border);
        margin: 0 15px;
      }

      .divider span {
        background-color: var(--card-bg);
        padding: 0 15px;
        color: var(--text-muted);
        font-size: 14px;
        font-weight: 500;
        position: relative;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .divider span i {
        color: var(--primary-color);
      }

      .questions-container {
        display: flex;
        flex-direction: column;
        gap: 20px;
      }

      .question-group {
        background-color: var(--card-inner);
        padding: 20px;
        border-radius: var(--border-radius);
        border: 1px solid var(--input-border);
        transition: var(--transition);
      }

      .question-group:hover {
        border-color: var(--primary-color);
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }

      .question-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 15px;
        border-bottom: 1px solid var(--input-border);
        padding-bottom: 12px;
      }

      .question-header label {
        font-size: 16px;
        color: var(--primary-color);
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .question-header .badge {
        background: linear-gradient(
          135deg,
          var(--primary-color) 0%,
          var(--accent-color) 100%
        );
        color: white;
        padding: 5px 10px;
        border-radius: 20px;
        font-size: 12px;
        font-weight: 500;
        display: flex;
        align-items: center;
        gap: 5px;
      }

      .question-group textarea {
        width: 100%;
        padding: 12px;
        border: 1px solid var(--input-border);
        color: var(--text-color);
        border-radius: 4px;
        font-family: inherit;
        font-size: 14px;
        line-height: 1.5;
        resize: vertical;
        background-color: var(--input-bg);
        transition: border-color 0.3s, box-shadow 0.3s;
        min-height: 120px;
      }

      .question-group textarea:focus {
        border-color: var(--primary-color);
        box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.2);
        outline: none;
      }

      .question-group textarea:hover {
        border-color: var(--primary-hover);
      }

      .update-btn {
        width: 100%;
        padding: 12px;
        background-color: var(--primary-color);
        color: white;
        border: none;
        border-radius: var(--border-radius);
        font-size: 16px;
        font-weight: 500;
        cursor: pointer;
        transition: var(--transition);
        margin-top: 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
      }

      .update-btn:hover {
        background-color: var(--primary-hover);
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
      }

      @media screen and (max-width: 768px) {
        .form-grid {
          grid-template-columns: 1fr;
        }
        
        .test-info {
          flex-direction: column;
          gap: 10px;
        }
      }
    </style>
  </head>
  <body>
    <div class="edit-container">
      <div class="edit-header">
        <h2><i class="fas fa-edit"></i> Edit Test</h2>
        <p>Update your test details</p>
        <button class="back-button" onclick="goBack()">
          <i class="fas fa-arrow-left"></i>
        </button>
      </div>

      <div class="test-info">
        <p><i class="fas fa-code"></i> <strong>Test Code:</strong> <%= test.code %></p>
        <p>
          <i class="fas fa-circle <%= test.status === 'Active' ? 'text-green-500' : 'text-red-500' %>"></i>
          <strong>Status:</strong> <%= test.status %>
        </p>
      </div>

      <div class="edit-form">
        <form action="/teacher/edit/<%= test.code %>" method="POST">
            <div class="form-grid">
              <div class="card">
                <div class="card-header">
                  <i class="fas fa-book"></i>
                  <h3>Basic Information</h3>
                </div>
                <div class="input-group">
                  <label for="subject">Subject</label>
                  <input
                    type="text"
                    id="subject"
                    name="subject"
                    value="<%= test.subject %>"
                    required
                  />
                  <i class="fas fa-book"></i>
                </div>
          
                <div class="input-group">
                  <label for="topic">Topic</label>
                  <input
                    type="text"
                    id="topic"
                    name="topic"
                    value="<%= test.topic %>"
                    required
                  />
                  <i class="fas fa-tag"></i>
                </div>
              </div>
          
              <div class="card">
                <div class="card-header">
                  <i class="fas fa-cog"></i>
                  <h3>Test Settings</h3>
                </div>
                <div class="input-group">
                  <label for="status">Status</label>
                  <select id="status" name="status" required>
                    <option value="active" <%= test.status === 'Active' ? 'selected' : '' %>>Active</option>
                    <option value="inactive" <%= test.status === 'Inactive' ? 'selected' : '' %>>Inactive</option>
                  </select>
                  <i class="fas fa-toggle-on"></i>
                </div>
                
                <div class="input-group">
                  <label for="testDuration">Duration (minutes)</label>
                  <input
                    type="number"
                    id="testDuration"
                    name="testDuration"
                    value="<%= test.duration || 60 %>"
                    min="1"
                  />
                  <i class="fas fa-clock"></i>
                </div>
              </div>
            </div>
          
            <div class="divider">
              <span><i class="fas fa-clipboard-list"></i> Questions</span>
            </div>
          
            <div class="questions-container">
              <% test.questions.forEach((question, index) => { %>
              <div class="question-group">
                <div class="question-header">
                  <label for="question<%= index %>">
                    <i class="fas fa-question-circle"></i> Question <%= index + 1 %>
                  </label>
                  <span class="badge">
                    <i class="fas fa-star"></i>
                    <select name="questionTypes[]">
                      <option value="1-marker" <%= question.type === '1-marker' ? 'selected' : '' %>>1-marker</option>
                      <option value="2-marker" <%= question.type === '2-marker' ? 'selected' : '' %>>2-marker</option>
                      <option value="5-marker" <%= question.type === '5-marker' ? 'selected' : '' %>>5-marker</option>
                    </select>
                  </span>
                </div>
                <textarea
                  id="question<%= index %>"
                  name="questions[]"
                  required><%= question.question %></textarea>
              </div>
              <% }) %>
            </div>
          
            <button type="submit" class="update-btn">
              <i class="fas fa-save"></i> Update Test
            </button>
          </form>
      </div>
    </div>

    <script>
      function goBack() {
        window.location.href = "/teacher/dashboard";
      }
    </script>
  </body>
</html>