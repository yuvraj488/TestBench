<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm Test</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #111827;
            --card-bg: #1f2937;
            --primary-color: #3b82f6;
            --primary-hover: #60a5fa;
            --text-color: #e5e7eb;
            --text-muted: #9ca3af;
            --input-bg: #374151;
            --input-border: #4b5563;
            --input-focus: #60a5fa;
            --border-radius: 8px;
            --shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.3), 0 4px 6px -2px rgba(0, 0, 0, 0.15);
            --red: #ef4444;
            --green: #10b981;
            --yellow: #f59e0b;
            --section-bg: #283548;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Roboto', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            padding: 20px;
        }
        
        .container {
            max-width: 900px;
            margin: 0 auto;
        }
        
        .header {
            background: linear-gradient(135deg, #374151 0%, #1e3a8a 100%);
            padding: 30px;
            text-align: center;
            color: white;
            border-radius: var(--border-radius) var(--border-radius) 0 0;
            position: relative;
        }
        
        .header h1 {
            font-weight: 600;
            font-size: 28px;
            margin-bottom: 5px;
        }
        
        .header p {
            color: var(--text-muted);
            margin-bottom: 10px;
        }
        
        .test-info {
            display: flex;
            justify-content: space-between;
            margin-top: 15px;
            background-color: rgba(0, 0, 0, 0.2);
            padding: 15px;
            border-radius: var(--border-radius);
        }
        
        .test-info-item {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        
        .test-info-item span:first-child {
            font-size: 14px;
            color: var(--text-muted);
        }
        
        .test-info-item span:last-child {
            font-size: 18px;
            font-weight: 500;
        }
        
        .content {
            background-color: var(--card-bg);
            padding: 30px;
            border-radius: 0 0 var(--border-radius) var(--border-radius);
            box-shadow: var(--shadow);
        }
        
        .section {
            margin-bottom: 30px;
            background-color: var(--section-bg);
            border-radius: var(--border-radius);
            padding: 20px;
            border-left: 4px solid;
        }
        
        .section-1-marker {
            border-left-color: var(--yellow);
        }
        
        .section-2-marker {
            border-left-color: var(--primary-color);
        }
        
        .section-5-marker {
            border-left-color: var(--green);
        }
        
        .section-title {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 1px solid var(--input-border);
        }
        
        .section-title h2 {
            font-size: 18px;
            font-weight: 500;
        }
        
        .badge {
            background-color: var(--input-bg);
            color: var(--text-color);
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 500;
        }
        
        .question-item {
            background-color: var(--card-bg);
            border-radius: var(--border-radius);
            padding: 15px;
            margin-bottom: 15px;
            border: 1px solid var(--input-border);
        }
        
        .question-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }
        
        .question-number {
            font-weight: 500;
            color: var(--text-muted);
        }
        
        .mark-badge {
            background-color: var(--primary-color);
            color: white;
            padding: 2px 8px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 500;
        }
        
        .question-text {
            margin-bottom: 15px;
        }
        
        .question-text textarea {
            width: 100%;
            min-height: 80px;
            background-color: var(--input-bg);
            border: 1px solid var(--input-border);
            border-radius: var(--border-radius);
            padding: 12px;
            color: var(--text-color);
            font-family: inherit;
            font-size: 14px;
            resize: vertical;
            transition: all 0.3s ease;
        }
        
        .question-text textarea:focus {
            outline: none;
            border-color: var(--input-focus);
            box-shadow: 0 0 0 3px rgba(96, 165, 250, 0.2);
        }
        
        .answer-section {
            margin-top: 15px;
        }
        
        .answer-label {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 8px;
        }
        
        .answer-label span {
            font-size: 14px;
            color: var(--text-muted);
        }
        
        .toggle-answer {
            background: none;
            border: none;
            color: var(--primary-color);
            font-size: 14px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 5px;
        }
        
        .answer-content {
            background-color: rgba(59, 130, 246, 0.1);
            border-radius: var(--border-radius);
            padding: 12px;
            font-size: 14px;
            border-left: 3px solid var(--primary-color);
            display: none;
        }
        
        .answer-content.show {
            display: block;
        }
        
        .answer-content textarea {
            width: 100%;
            min-height: 80px;
            background-color: rgba(59, 130, 246, 0.05);
            border: 1px solid rgba(59, 130, 246, 0.2);
            border-radius: var(--border-radius);
            padding: 12px;
            color: var(--text-color);
            font-family: inherit;
            font-size: 14px;
            resize: vertical;
        }
        
        .answer-content textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(96, 165, 250, 0.2);
        }
        
        .button-container {
            display: flex;
            justify-content: space-between;
            margin-top: 40px;
            gap: 20px;
        }
        
        .btn {
            padding: 12px 20px;
            border-radius: var(--border-radius);
            font-size: 16px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            flex: 1;
        }
        
        .btn-back {
            background-color: var(--input-bg);
            color: var(--text-color);
            border: 1px solid var(--input-border);
        }
        
        .btn-back:hover {
            background-color: var(--section-bg);
        }
        
        .btn-confirm {
            background-color: var(--primary-color);
            color: white;
            border: none;
        }
        
        .btn-confirm:hover {
            background-color: var(--primary-hover);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
        }
        
        .btn-confirm:active {
            transform: translateY(0);
        }
        
        .empty-section {
            text-align: center;
            padding: 20px;
            color: var(--text-muted);
        }
        
        @media (max-width: 768px) {
            .test-info {
                flex-direction: column;
                gap: 15px;
            }
            
            .test-info-item {
                flex-direction: row;
                justify-content: space-between;
                width: 100%;
            }
            
            .button-container {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <% 
// Ensure test.questions is an array
const questionsArray = Array.isArray(test.questions) ? test.questions : [];

// Group questions by mark value
const oneMarkerQuestions = questionsArray.filter(q => parseInt(q.marks) === 1);
const twoMarkerQuestions = questionsArray.filter(q => parseInt(q.marks) === 2);
const fiveMarkerQuestions = questionsArray.filter(q => parseInt(q.marks) === 5);

// Helper function to render question section
function renderQuestionSection(questions, sectionTitle, markCount, sectionClass) {
    if (questions.length === 0) return '';
    
    let html = `
    <div class="section ${sectionClass}" id="section-${markCount}-marker">
        <div class="section-title">
            <h2>${sectionTitle}</h2>
            <span class="badge">${markCount} mark${markCount > 1 ? 's' : ''} each</span>
        </div>
        <div id="${markCount}-marker-questions">`;
    
    questions.forEach((question, index) => {
        html += `
        <div class="question-item">
            <div class="question-header">
                <span class="question-number">Question ${index + 1}</span>
                <span class="mark-badge">${question.marks} mark${question.marks > 1 ? 's' : ''}</span>
            </div>
            <div class="question-text">
                <textarea name="questions[${question.id}][question]" rows="3">${question.question}</textarea>
                <input type="hidden" name="questions[${question.id}][type]" value="${question.type}">
                <input type="hidden" name="questions[${question.id}][marks]" value="${question.marks}">
            </div>
            <div class="answer-section">
                <div class="answer-label">
                    <span>Model Answer</span>
                    <button type="button" class="toggle-answer" onclick="toggleAnswer('answer-${question.id}')">
                        <i class="fas fa-eye"></i> Show/Hide
                    </button>
                </div>
                <div id="answer-${question.id}" class="answer-content">
                    <textarea name="questions[${question.id}][answer]" rows="4">${question.answer}</textarea>
                </div>
            </div>
        </div>`;
    });
    
    html += `
        </div>
    </div>`;
    
    return html;
}
%>
    <div class="container">
        <div class="header">
            <h1>Confirm Test</h1>
            <p>Review and edit questions before finalizing</p>
            
            <div class="test-info">
                <div class="test-info-item">
                    <span>Subject</span>
                    <span id="subject-name"><%= test.subject %></span>
                </div>
                <div class="test-info-item">
                    <span>Topic</span>
                    <span id="topic-name"><%= test.topic %></span>
                </div>
                <div class="test-info-item">
                    <span>Total Marks</span>
                    <span id="total-marks"><%= test.totalMarks %></span>
                </div>
            </div>
        </div>
        
        <div class="content">
            <form id="finalizeTestForm" action="/teacher/finalize-test" method="post">
                <input type="hidden" id="subject-input" name="subject" value="<%= test.subject %>">
                <input type="hidden" id="topic-input" name="topic" value="<%= test.topic %>">
                    <%- renderQuestionSection(oneMarkerQuestions, '1-Marker Questions', 1, 'section-1-marker') %>
                    <%- renderQuestionSection(twoMarkerQuestions, '2-Marker Questions', 2, 'section-2-marker') %>
                    <%- renderQuestionSection(fiveMarkerQuestions, '5-Marker Questions', 5, 'section-5-marker') %>

                
                <div class="button-container">
                    <button type="button" class="btn btn-back" onclick="goBack()">
                        <i class="fas fa-arrow-left"></i> Back to Edit
                    </button>
                    <button type="submit" class="btn btn-confirm">
                        <i class="fas fa-check"></i> Confirm Test
                    </button>
                </div>
            </form>
        </div>
    </div>

    <script>
        function toggleAnswer(answerId) {
            const answerElement = document.getElementById(answerId);
            answerElement.classList.toggle('show');
        }
        function goBack() {
            window.location.href = '/teacher/aitest';
        }
    </script>
</body>
</html>